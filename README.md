### Library API using .NET 8

# Requirements:

- .NET 8 SDK


# Usage:

- `dotnet run -lp https`
- `ctrl` + click the localhost url
- add `/swagger` in the rest of url. (example the url `https://localhost:8080` to  `https://localhost:8080/swagger`)